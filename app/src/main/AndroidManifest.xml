<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clcu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="mainview.activity_todolist"></activity>
        <activity android:name="mainview.MainActivity" />
        <activity android:name="mainview.MyPage" />
        <activity android:name="mainview.BookmarkPage" />
        <activity android:name="content.Main13Activity" />
        <activity android:name="mainview.TipPageB" />
        <activity android:name="mainview.TipPage" />
        <activity android:name="mainview.CalanderPage" />
        <activity
            android:name="start.SplashScreen"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name="start.SplashScreen"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name="start.Main8Activity" />
        <activity android:name="start.Main7Activity" />
        <activity android:name="start.Main6Activity" />
        <activity android:name="start.Main5Activity" />
        <activity android:name="start.Main4Activity" />
        <activity android:name="start.Main3Activity" />
        <activity android:name="start.Main2Activity" />
    </application>

</manifest>